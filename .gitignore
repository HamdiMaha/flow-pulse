node_modules
dist
.DS_Store
*.local
.vite
*.tsbuildinfo
.env
.env.*
!.env.example

# Google service-account keys — never commit real credentials.
*service-account*.json
*.key.json
gcp-*.json

# A local directory junction/symlink into a synced SharePoint folder —
# machine-specific, and its contents (someone else's QA data) don't
# belong in this repo. See flows/README.md.
/flows/sharepoint/

# Real QA data — kept local only, not pushed to this public repo.
/flows/PT5282.csv
